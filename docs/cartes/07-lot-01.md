---
carte: 07
nom: Lot 01
agent: dev
skills: [vibe-coding-cadre, tests-dabord]
parents: [06]
statut: approuvee
livrable: src/domain/reference.ts, src/domain/etoiles.ts
porte: "CA-04, CA-05, CA-07, CA-08 verts ; typecheck et lint propres sur le périmètre du lot ; docs/ intact"
risque: L0
approuvee_le: 2026-09-13T21:24:20+00:00
veto_jusqu_au: 2026-09-14T21:24:20+00:00
---

## Entrées reçues

`docs/lots.md` (lot 1), `docs/spec.md`, `docs/data-model.md`, les tests rouges de
`test/reference.test.ts` et `test/etoiles.test.ts`. Rien ne manquait.

## Résumé de fin de carte

- **Action proposée** : approuver le lot 1 ; le lot 2 peut être lancé en loop.
- **Ce qui change** : `src/domain/reference.ts` et `src/domain/etoiles.ts` existent,
  testés, verts. Le détail signature du produit (les chiffres n'existent pas, seule la
  note en étoiles existe) est maintenant vérifiable par du code, dès le lot 1, comme
  l'exige la règle de découpage.
- **Sources** : `docs/spec.md`, `docs/data-model.md`.
- **Ce qui manque** : rien pour ce lot.
- **Risque** : L0, code pur, aucun effet de bord, aucun accès réseau.
- **À trancher** : aucune décision réservée.
- **Coût du run** : loop contexte frais, 1 itération sur 8 allouées.

## Ce que le loop a produit, réellement

Exécuté via `bash .loop/ralph.sh lot-01 8`, une seule itération a suffi :

- `referenceGlissante` : médiane des six dernières charges, sans distinction de semaine (CA-05).
- `calculerEtoiles` : silence de calibration sous deux séances (CA-04), seuils exacts (CA-07),
  plafond à 4 étoiles après deux efforts consécutifs à 10 (CA-08).
- `npm test` sur la branche : 10/10 tests passent (health + les 4 du lot), 0 régression.
- `npm run typecheck` / `npm run lint` : propres sur le périmètre du lot ; les erreurs
  restantes concernent des modules des lots 2 à 5, non encore écrits, hors périmètre.
- `git diff --stat main -- docs/` : vide.
- Une ambiguïté mineure notée par l'agent : les bornes exactes des seuils (0.6, 0.9, 1.1,
  1.4) ne sont pas testées à la limite précise dans `docs/spec.md`. Convention choisie
  (borne basse incluse dans la tranche supérieure), documentée dans
  `.loop/lot-01-progress.md`, sans impact sur les tests existants.

## Ce qui a bloqué, et comment c'est tranché

Le loop s'est arrêté honnêtement (`STATUS: LOT_01_BLOCKED`, `BLOCKED.md`) faute de dépôt
GitHub distant pour ouvrir la PR prévue par `vibe-coding-cadre`. Ce n'est pas un défaut du
code, confirmé par une relecture indépendante des tests et du diff avant cette carte.

**Décision** : en l'absence de dépôt distant pour ce projet à ce stade, la branche
`lot-01` est fusionnée directement dans `main` en local (commit `c7fd1fa`), au lieu
d'une PR. C'est un manquement à la règle « une PR par lot » de `PROCESS.md`, accepté une
fois pour cause d'infrastructure manquante, pas de qualité douteuse : les mêmes
vérifications ont été refaites avant la fusion. Dès qu'un dépôt distant existe, les lots
suivants suivent la règle normale (PR + CI).

## Journal de décision

- **Décision** : lot 1 approuvé ; fusionné dans `main` en local faute de dépôt distant.
- **Options considérées** : (a) attendre que le fondateur crée un dépôt GitHub avant de
  continuer, retardant tout ; (b) fusionner en local maintenant, en le documentant comme
  écart accepté, et garder les lots suivants derrière la vraie règle de PR dès que le
  dépôt existe (retenue) ; (c) considérer le lot comme non terminé tant qu'il n'y a pas
  de PR, écartée car le code est vérifié vert et la seule chose manquante est un moyen
  d'héberger la revue, pas la revue elle-même.
- **Qui a tranché** : Stratège. Aucune décision de la liste réservée n'est concernée
  (fusionner du code déjà écrit et testé n'est ni publier, ni envoyer à un tiers).
- **Réversible** : oui, `git revert` du commit de fusion si besoin.
- **Ce qui ferait revenir dessus** : si une régression liée à ce lot apparaît au lot 2.
- **Veto possible jusqu'à** : 2026-09-14T21:24:20+00:00.

## metadata
```json
{
  "criteres_couverts": ["CA-04", "CA-05", "CA-07", "CA-08"],
  "iterations": 1,
  "iterations_max": 8,
  "tests_avant": "0/1 (seul health vert)",
  "tests_apres": "10/10 dans le périmètre du lot (12 suites totales, 9 hors périmètre encore rouges)",
  "ecart_process": "fusion locale au lieu d'une PR, faute de dépôt distant ; à corriger dès que le dépôt existe"
}
```
