---
carte: 08
nom: Lot 02
agent: dev
skills: [vibe-coding-cadre, tests-dabord]
parents: [07]
statut: approuvee
livrable: src/db/in-memory.ts, src/domain/consentement.ts, src/domain/parsing.ts, src/domain/message.ts, src/telegram/webhook.ts
porte: "CA-01, CA-02, CA-03, CA-06 verts ; typecheck et lint propres ; docs/ intact"
risque: L0
approuvee_le: 2026-09-14T07:22:06+00:00
veto_jusqu_au: 2026-09-15T07:22:06+00:00
---

## Entrées reçues
`docs/lots.md` (lot 2), les stubs typés du lot 1, les tests rouges correspondants. Rien ne manquait.

## Résumé de fin de carte
- **Action proposée** : approuver le lot 2 ; PR #7 fusionnée dans `main`.
- **Ce qui change** : `InMemoryRepository` implémenté (retiré des stubs), consentement,
  analyse de la déclaration, mise en forme du message public (aucun chiffre), squelette
  du webhook Telegram.
- **Sources** : `docs/spec.md`, `docs/data-model.md`.
- **Ce qui manque** : rien pour ce lot.
- **Risque** : L0.
- **À trancher** : rien de réservé. Point noté par le Contradicteur du loop, non
  bloquant : une activité déclarée en texte libre pourrait contenir un chiffre (ex.
  « vélo 10 km ») qui traverserait `formaterMessagePublic` sans être filtré, puisque
  CA-06 ne teste que l'absence de chiffre côté étoiles, pas côté nom d'activité. Réel
  mais hors périmètre testé de ce lot ; transmis pour un lot futur ou la recette.
- **Coût du run** : loop contexte frais, 4 itérations sur 20 allouées.

## Ce que le loop a produit, réellement
`bash .loop/ralph.sh lot-02 20`, terminé en 4 itérations : `InMemoryRepository`,
`consentement.ts`, `parsing.ts`, `message.ts`, squelette `webhook.ts`. Revue
contradictoire faite par le loop lui-même (agent `regie:contradicteur` non installé
dans cet environnement headless) : diff relu, deux points documentés sans correction
(méthodes ajoutées à `InMemoryRepository` bien demandées par la mission ; le risque
d'un chiffre dans une activité libre, noté ci-dessus). PR #7 ouverte par l'orchestrateur
via l'API GitHub (`gh` indisponible dans cet environnement), CI verte, fusionnée.

## Journal de décision
- **Décision** : lot 2 approuvé et fusionné par PR (première PR réelle du projet,
  le dépôt distant existant désormais).
- **Options considérées** : (a) fusion directe comme le lot 1 (écartée, le dépôt
  distant existe maintenant) ; (b) PR avec CI, retenue.
- **Qui a tranché** : Stratège.
- **Réversible** : oui, revert de la PR.
- **Ce qui ferait revenir dessus** : une régression sur les tests du lot 1 ou 2.
- **Veto possible jusqu'à** : 2026-09-15T07:22:06+00:00.

## metadata
```json
{
  "criteres_couverts": ["CA-01", "CA-02", "CA-03", "CA-06"],
  "iterations": 4, "iterations_max": 20,
  "pr": "https://github.com/Lugdunoob/coeur-relatif/pull/7",
  "point_transmis": "un chiffre dans une activité libre n'est pas filtré par CA-06 ; à couvrir en recette ou lot futur"
}
```
